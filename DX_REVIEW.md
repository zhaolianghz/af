# DX Review — AF Selector
Generated by /plan-devex-review on 2026-06-17
Mode: DX POLISH (bulletproof every touchpoint)

## TL;DR — 当前 DX 评分: **3.6 / 10**

**Severity overview:**

| 严重度 | 数量 | 主题 |
|--------|------|------|
| P0 致命 | 2 | CI 配置错(Makefile / scripts 文档全是谎言) |
| P1 高 | 3 | 无 OpenAPI、错误信息不友好、`.env.example` 缺失 |
| P2 中 | 2 | 无 Makefile,跨平台体验缺失 |

**核心问题:README 大量描述了不存在的命令和文件**。新开发者按 README 走会卡在第一步。

---

## Pass 1: Getting Started (3/10 → 目标 10/10)

### 实际跑通流程(我刚走过的)

```bash
# README §4 Quick Start 声称:
git clone <repo>
cd af
cp .env.example .env                  # ✗ .env.example 不存在
cp backend/configs/config.example.yaml backend/configs/config.yaml  # ✓ 这个有
make tidy                            # ✗ Makefile 不存在
make dev-up                          # ✗ docker-compose.yml 不存在
make run-backend                     # ✗
make run-frontend                    # ✗
open http://localhost:5173            # 前端 dev 是 :5173,但后端跑在 :8080
curl http://localhost:8080/healthz    # 后端 :8080(没有 nginx)
```

**每一行 `make` 命令都失败。** README 承诺的快乐路径根本不存在。

### 缺失的入口

| 入口 | README 声称 | 实际 |
|------|------------|------|
| `make help` | §11 列出 14 个 target | 不存在 |
| `make dev-up` | 启动 mysql+redis | 不存在 |
| `make dev-down` | 关掉 mysql+redis | 不存在 |
| `make run-backend` | go run | 不存在 |
| `make run-frontend` | vite dev | 不存在 |
| `make build` | 同时构建两端 | 不存在 |
| `make test` | go test + npm test | 不存在 |
| `make deploy` | scripts/deploy.sh | 不存在 |
| `scripts/deploy.sh` | §9 声称存在 | 不存在 |
| `scripts/smoke.sh` | §11 声称存在 | 不存在 |
| `scripts/check.sh` | §11 声称存在 | 不存在 |
| `scripts/dev-up.sh` | §9 引用 | 不存在 |
| `docker compose` | §5 声称存在 | docker-compose.yml 不存在 |
| `docker-compose.yml` | §5 应该有 | 不存在 |
| `Dockerfile` | §9 引用 | 不存在 |
| `.env.example` | §4 引用 | 不存在 |
| `backend/.env.example` | (推测) | 不存在(只有 config.example.yaml) |
| `frontend/.env.example` | (推测) | 存在 |

### 真正能跑通的本地开发流程(我自己摸索出来的)

```bash
# Backend
cd backend
go mod download
DB_USER=af DB_PASSWORD=afpass DB_NAME=af \
  go run ./cmd/server -config configs/config.example.yaml
# → 后端跑在 :8080,DB 报错,需先建 DB

# Frontend
cd frontend
npm install
npm run dev
# → 前端跑在 :5173,代理到 :8080
```

**问题是:没有 `.env.example` 在 root,新开发者只能看 `config.example.yaml`,而 `config.example.yaml` 默认 driver=mysql 需要 MySQL,密码写死 root/root。**

### Time To Hello World(TTHW)

- README 声称:"5 minutes"
- 实际:**20+ 分钟**(踩坑 GORM 1.25 复合 uniqueIndex bug、MySQL 密码、CNAT 端口映射)
- TTHW 差距:**4 倍**

### FIX 路线 → 10/10

1. **创建 `Makefile`**,实现 README §11 列出的 14 个 target。
2. **创建 `docker-compose.yml`**,包含 `mysql:8.0` + `redis:7-alpine` + `app`(Go)+ `web`(Nginx)。
3. **创建 `Dockerfile`**,多阶段构建。
4. **创建 `scripts/{deploy,smoke,check,dev-up}.sh`**,实现 §9 的部署流程。
5. **创建根 `.env.example`**,列出所有 env vars(MYSQL_PASSWORD / REDIS_HOST / TUSHARE_TOKEN 等)。
6. **修 README**,只描述实际存在的命令。
7. **加 `docs/QUICKSTART.md`**,纯 shell 步骤,3 步 5 分钟到 hello world。

---

## Pass 2: API Design (7/10)

### 优点

- ✓ **统一的 envelope**:`{ code: 0, message: "ok", data: {...} }` 在 `apperr` 包,所有 API 一致。
- ✓ **HTTP status code 映射**:`apperr.HTTPStatus(c Code) int` 把内部 code 翻译成 HTTP status。
- ✓ **分页统一**:`{ items, total, page, page_size }` 结构在 strategies、runs、recommendations 通用。
- ✓ **RESTful**:GET 列表 / GET 详情 / POST 创建 / PUT 更新 / DELETE 删除 命名一致。
- ✓ **嵌套资源清晰**:`/api/v1/strategies/:id/trial-run`、`/api/v1/runs/:id/events` 表达力强。
- ✓ **SSE 支持 Last-Event-ID 续传**:从设计上考虑了断线重连。
- ✓ **错误码稳定**:`Code` 是 int 常量,新增错误不会改老 code。

### 缺点

- ✗ **无 OpenAPI/Swagger spec**。新开发者只能挖 `router.go` 或读 handler 注释才知道有哪些端点。`getRunLogs` `/getRun` 命名都合理但要读代码才知道参数。
- ✗ **`/healthz` 不在 `/api/v1` 下**(`r.GET("/healthz", ...)` 在根),而其他端点都在 `/api/v1/`。**不一致**。
- ✗ **错误响应没有 `request_id` / `trace_id`**。生产出问题时,没法把客户端错误和服务端日志关联起来。
- ✗ **错误响应没有 `details` 字段**。`"recommendation not found"` 不告诉调用方缺什么(`?id=123` 还是 `?code=ma_breakout`)。
- ✗ **没有 `Link` 头分页 RFC 5988**(只有 page/page_size 数字,没法深链)。
- ✗ **没有 `OpenAPI`/`AsyncAPI` 文档**。前端开发靠 TypeScript 类型 `types/orchestrator.ts`,这在 TS 重构时是单一真相,但对其他语言不友好(没有 Python SDK、Go SDK 之类)。
- ✗ **Trial-run endpoint 路径不一致**:`/api/v1/strategies/:id/trial-run` vs `/api/v1/strategies/:id/trial-run/node/:nodeId` 用了 snake_case 嵌 nodeId,部分端点用 `?strategy_id=` 而 `trial-run` 用 path param。

### FIX 路线 → 10/10

1. **加 swaggo/swag 自动生成 OpenAPI 3.0 spec** 在 `/api/v1/openapi.json`。
2. **统一 healthz**:`/api/v1/healthz` + 保留 `/healthz` 作 alias。
3. **BizError 加 `request_id` 和 `details` 字段**。
4. **分页加 `Link` 头**。
5. **加 `?fields=` 参数支持 sparse fieldsets**(性能优化)。
6. **生成 Postman / Insomnia collection** 同步发布。

---

## Pass 3: Error Messages (4/10)

### 真实事故复盘(我刚遇到的)

**场景:部署时 GORM AutoMigrate 报 "Duplicate column name 'source'"**

**用户(我)看到的错误:**

```
{"level":"error","ts":"...","caller":"server/main.go:89","msg":"init database failed","error":"migrate db: Error 1060 (42S21): Duplicate column name 'source'"}
```

**问题:**
- 错误信息**没说是哪个表的哪一列**。我得自己跑 `DESCRIBE recommendation_tags;` 才能定位。
- 错误信息**没说是哪个 GORM 模型定义**。要扫所有 `.go` 文件 grep `Source` 找源头。
- 错误信息**没给 fix suggestion**。要翻 GORM 文档/源码才知道是 `uniqueIndex + index` 复合 tag 的 bug。
- **重复重启 6 次才偶然发现 docker volume 有缓存**(其实没有,但谁也不知道)。

### 当前错误响应的优点

- ✓ 有 `code` 数字 + `message` 文本,前端可读。
- ✓ `apperr.CodeNotFound` / `CodeConflict` / `CodeInvalidArg` 等分类清晰。
- ✓ `apperr.Wrap(code, msg, cause)` 支持 wrap 底层 error,Go 风格友好。
- ✓ Go 后端日志有 `caller` 字段(`server/main.go:89`),定位文件+行号。

### 缺点(严重)

- ✗ **错误响应无 `request_id`**。客户端报"请求失败了"和服务器日志"某处发生 500"对不上。
- ✗ **错误响应无 `details` 字段**。`"strategy not found"` 不告诉调用方传了什么 id。
- ✗ **错误响应无 `doc_url`**。"内部"错误应该有"这是怎么发生的,怎么避免"的链接。
- ✗ **后端日志写到 JSON 文件 `af.log`**,但 stderr/stdout 没暴露 — 容器化部署看不到 panic stack。
- ✗ **stack trace 仅在 GORM 这种外部库出错时**;自家代码 panic 没专门 handler,只走 `gin.Recovery()` 默认中间件(看着 stock)。
- ✗ **没有 `version` 字段**。错误响应没附软件版本,bug 报告时无法定位。

### FIX 路线 → 10/10

1. **生成 `request_id`(UUID v4)**:每个请求一个,放 `X-Request-ID` header + 错误响应 + 日志。
2. **加 `details` 字段**:`apperr` 加 `Details map[string]any`。
3. **加 `doc_url`**:错误码映射到 docs/errors/<code>.md(可放项目里 `docs/errors/`)。
4. **Gin 中间件 panic handler**:捕获 panic 后,返回带 request_id + safe internal error,不漏 stack 给客户端,但记全 stack 到日志。
5. **GORM migration error 翻译层**:`database.Migrate` 返回的 error 翻译成"table X column Y conflict with tag Z"的人类可读消息。

---

## Pass 4: Documentation (5/10)

### 优点

- ✓ **README 35KB / 683 行 / 13 章节** — 规模大。
- ✓ §1-13 覆盖 What / Architecture / Tech Stack / Quick Start(2 个)/ API Surface / Project Structure / Roadmap / Deployment / Development / Makefile / Contributing / License。
- ✓ **`openspec/` 目录**:有 `design.md`、`tasks.md`、`proposal.md`、`specs/`(10 个 spec 目录)。
- ✓ **frontend/src/types/orchestrator.ts**:380 行的 TypeScript 类型定义 = TypeScript 客户端的真实 API 文档。
- ✓ **PR #1 review** 的 TODOS.md 跟踪 post-merge cleanup(M1/M2/M3)。
- ✓ **注释充分**:GORM model 字段有 inline 注释,handler 路由有简短说明。

### 缺点(严重)

- ✗ **README §11 描述了一个不存在的 Makefile**。14 个 target 全是幽灵。
- ✗ **README §5 描述了 docker-compose 流程,但 docker-compose.yml 不存在**。
- ✗ **README §9 引用了 scripts/deploy.sh,不存在**。
- ✗ **无 OpenAPI / Swagger UI**。`/api/v1/openapi.json` 不存在,Swagger UI 不存在。
- ✗ **无 API CHANGELOG**。版本间的 breaking changes 没法追溯。
- ✗ **无 `docs/ARCHITECTURE.md`** — README §2 有图,但深度不够,没有 sequence diagram / data model diagram / deployment topology。
- ✗ **无 `docs/OPERATIONS.md`** — 没有 runbook 解释"DB 连接不上怎么办"、"SSE 不工作怎么 debug"、"job 卡死怎么 kill"。
- ✗ **无 CONTRIBUTING.md** — README §12 有但很简略,没说明 commit message 规范、PR 流程、code review 期望。
- ✗ **无 CODE_OF_CONDUCT.md / SECURITY.md** — 内部工具但也该有。

### FIX 路线 → 10/10

1. **删除 README §11/§9 中所有不存在的命令**,或实际创建 Makefile。
2. **加 `docs/ARCHITECTURE.md`**:ASCII 图 + sequence diagram + decision records。
3. **加 `docs/OPERATIONS.md`** runbook:常见故障 + 诊断步骤。
4. **加 `docs/CHANGELOG.md`** 跟踪 API breaking changes。
5. **加 `docs/CONTRIBUTING.md`** + `docs/SECURITY.md`。
6. **生成 OpenAPI JSON** + Swagger UI 在 `/docs`。

---

## Pass 5: Upgrade & Migration (N/A for v0.1)

v0.1 项目,无用户,暂不评分。**但当 v0.2 开始时**:
- 没有 `migrations/` 目录(GORM AutoMigrate 是仅有的 schema 演化)。
- 没有 `schema_versions` 表(无从知道当前 DB schema 是哪个版本)。
- 没有 deprecation 流程。

**FIX:** 加 `golang-migrate/migrate` 显式 SQL migrations,版本号时间戳命名。

---

## Pass 6: Developer Environment (3/10 — 致命)

### 致命问题:CI 完全 broken

`.github/workflows/ci.yml` line 23-24:

```yaml
matrix:
  go-version: ["1.22", "1.23"]
```

但 `backend/go.mod` line 3:

```
go 1.25.0
```

**CI 跑任何 Go 测试都会失败**:Go 1.22 不知道 `go 1.25.0` 指令(GOFLAGS=-mod=mod),会报 `go.mod requires go 1.25.0 (running go 1.22.5)`。

**修复:** CI 改成 `["1.25"]`,或加个 lint step 检查 go.mod 与 CI matrix 一致。

### 其他问题

- ✗ **无 Makefile**(README 撒谎)。日常命令 `cd backend && go test ./...` 太啰嗦。
- ✗ **无 hot-reload**。Go 后端改了代码要 `go run` 重启,frontend `npm run dev` 改一行就 reload 体验好很多。
- ✗ **无 pre-commit hook**(`scripts/check.sh` 声称有但不存在)。
- ✗ **无 `devcontainer.json`** / `.devcontainer/` — VSCode Remote Containers 用户体验差。
- ✗ **无 `Dockerfile`** — 跨机器部署依赖手工复制。
- ✗ **无 `Makefile` cross-compile**。我刚手敲 `GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build` 4 个 env var。
- ✗ **CI 没有 cache**:go modules 每次都重下、npm packages 每次都重装。
- ✗ **CI 不知道前端要怎么跑** — ci.yml 只有 backend 步骤,没前端 lint / typecheck / e2e。**前端没有 CI 保护!**

### FIX 路线 → 10/10

1. **修 CI**:`go-version: ["1.25"]`,加 frontend job(lint + typecheck + e2e)。
2. **创建 Makefile** with `tidy/build/test/lint/dev-up/dev-down/deploy/help`。
3. **创建 Dockerfile + docker-compose.yml**。
4. **加 pre-commit hook**:`make check` 跑 lint + format。
5. **加 CI cache**:`actions/setup-go@v5` 已支持,但 `actions/setup-node@v4` 没配,加。
6. **加 devcontainer.json** 给 VSCode 用户。
7. **自动检测 go.mod 版本与 CI 不一致**的 lint step:`! grep "matrix.*go-version" .github/workflows/ci.yml | grep -q "$(grep '^go ' backend/go.mod | awk '{print $2}')"` 时 fail。

---

## Pass 7: Community (N/A)

这是内部工具,不开源,无外部社区。但**对内**:
- ✗ 无 `#af-deploy` Slack 频道约定
- ✗ 无团队 wiki
- ✗ 无内部 demo 视频

**FIX:** 内部使用可以靠 README 够用,但建议加 `docs/FAQ.md` 收集运行中遇到的问题。

---

## Pass 8: Measurement (1/10)

✗ 没有 TTHW 测量
✗ 没有 journey analytics
✗ 没有"开发者反馈"机制
✗ 没有 friction audit

**FIX(最简版):**
1. 部署时记录"用户首次 curl /api/v1/healthz 成功"到 `af-deploy-success.log`,用 timestamp diff 算 TTHW。
2. 加 `/api/v1/feedback` endpoint,允许开发者报错时 POST `{"message": "..."}`,写入 `af-feedback.log`,每周 review。
3. 季度性 friction audit:让一个新成员装环境,记时间和卡点。

---

## 全局修复优先级(由 P0 → P2)

### P0 — 立即修

1. **CI 修 Go 版本**:`backend/go.mod` 要 1.25,CI matrix 写 1.22/1.23 → 改 `["1.25"]`。
2. **修 README §11**:删掉 Makefile 描述,或实际创建 Makefile。
3. **修 README §5**:删掉 docker-compose 描述,或实际创建 `docker-compose.yml`。

### P1 — 本周修

4. **加 `Makefile`**:14 个 target,真实现,替代 README 的谎言。
5. **加 `docker-compose.yml`**:mysql + redis + app + web,真能跑。
6. **加 `.env.example`** at root。
7. **加 `docs/OPERATIONS.md`**:runbook。
8. **加 OpenAPI spec** 自动生成 + Swagger UI。

### P2 — 下周修

9. **加 `request_id` 到所有 API 响应和日志**。
10. **加 CHANGELOG.md**。
11. **加 devcontainer.json**。
12. **加 pre-commit hook**。

### 工作量估算(CC + gstack)

- P0: **30 分钟** — 3 行 CI 改 + README 删 30 行
- P1: **4-6 小时** — Makefile + docker-compose + .env.example + docs
- P2: **2-3 小时** — 各种文档

**总:1 个工作日内把 DX 拉到 8/10。**

---

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| DX Review | `/plan-devex-review` | 部署后 onboarding 体验评估 | 1 | `ISSUES_OPEN` | 3 P0 + 5 P1 + 2 P2 |
| Eng Review | — | — | 0 | — | M4 eng review 推动的 P1/P2 修复全完成 |
| CEO Review | — | — | 0 | — | FE1 + FE2 计划已审查 |
| Design Review | — | — | 0 | — | 待运行 |
| Outside Voice | — | — | 0 | — | 用户跳过 |

**VERDICT:** DX REVIEW ISSUES OPEN — 3 个 P0 致命(CI broken + README 谎言)必须先修。修完 P0 后可拉到 6/10,完成 P1 后到 8/10。

**Mode:** DX POLISH | **P0:** 3 | **P1:** 5 | **P2:** 4
