# FE2 Plan — 运行历史 + 实时 SSE 日志

> Generated by Claude Code, 2026-06-17
> Branch: `main` | Mode: SELECTIVE EXPANSION
> Source: FE1_PLAN.md §12, M4 提交后启动

## TL;DR

FE2 前端 + 后端 **已基本就位**(M1-M4 范围内已交付,见 §1 审计)。这一阶段的工作**不是从零搭建**,而是:
1. **补测试覆盖**:RunStatusBadge / RunTimeline / LogStreamViewer / RunHistoryPage / RunDetailPage 全部 0 测试
2. **e2e 烟雾测试**:列表 → 详情 → 实时日志流;手工触发 → 重定向到详情
3. **M4 风格打磨**:把残留的 `alert()` / 静默吞错 / 不一致的反馈接入 M4 新基础件(toast / ConfirmDialog)
4. **可选打磨**:重连 UX、终端状态过渡、键盘快捷键

预计 **3-4 天 CC 投入** / **1 周人投入**,分 4 个里程碑交付。

---

## 1. 现状审计(Audit Summary)

### 1.1 已存在的 FE2 文件

经过对 `frontend/src/` + `backend/internal/executor/` 的全面审计,FE2 全栈基本完整:

| 组件 | 文件 | 状态 |
|------|------|------|
| 列表页 | `pages/RunHistoryPage.tsx` (409 行) | ✅ 完整:表格 / 筛选 / 分页 / 手工触发模态框 |
| 详情页 | `pages/RunDetailPage.tsx` (217 行) | ✅ 完整:元数据 / 重试(已用 useConfirm) / 时间线 + 日志流 |
| 状态徽章 | `components/runs/RunStatusBadge.tsx` | ✅ 完整 |
| 节点时间线 | `components/runs/RunTimeline.tsx` | ✅ 完整 |
| 实时日志 | `components/runs/LogStreamViewer.tsx` (245 行) | ✅ 完整:EventSource / 暂停 / 重连 / 自动滚动 / 2000 事件上限 |
| API 客户端 | `api/runs.ts` (137 行) | ✅ 完整:CRUD + `openRunEventStream` |
| 后端 SSE | `backend/internal/executor/handler.go:256` | ✅ 完整:`Content-Type: text/event-stream` |
| 后端测试 | `handler_test.go` | ✅ 88+ tests |

**结论**:FE2 的 8 个核心文件已完整实现。无需重建。

### 1.2 已实现的能力

- ✅ 运行历史列表(按状态/方案筛选 + 分页)
- ✅ 手工触发运行模态框(方案下拉)
- ✅ 运行详情页(元数据 + 状态 + 重试 + 节点时间线 + 实时日志)
- ✅ SSE 实时事件流(EventSource 自动重连 + 暂停 + 重连按钮 + 清空 + 2000 事件上限 + 自动滚到底)
- ✅ 后端 SSE endpoint 推送 9 种事件类型:ready/heartbeat/run.started/run.completed/node.* / log
- ✅ 后端 5 秒轮询兜底(防 EventSource 漏掉终端事件)

### 1.3 已有的空白(Gaps)

| 缺口 | 严重度 | 影响 |
|------|--------|------|
| **RunStatusBadge / RunTimeline / LogStreamViewer 0 单测** | P0 | 任何 UI 改动都是裸奔,无回归保护 |
| **RunHistoryPage / RunDetailPage 0 单测** | P0 | 列表筛选/分页逻辑 + 重试/触发 流程无单测 |
| **e2e 0 个 runs 烟雾测试** | P1 | 列表 → 详情 → 日志流 + 手工触发 无 e2e |
| **M4 风格打磨不完整** | P1 | 手工触发模态框用裸 `error` 渲染,M4 之后应该用 toast |
| **后端 SSE 集成测试** | P2 | handler_test.go 有路由表但 SSE frame 解析未测 |
| **a11y 键盘焦点** | P2 | 列表页筛选下拉 / 详情页重试按钮的键盘可达性未审 |
| **LogStreamViewer 暂停状态高亮** | P3 | 暂停时无视觉提示,仅文字 |
| **运行详情页缺 keyboard shortcut** | P3 | 无 Esc 回列表、Ctrl+R 重试 |

---

## 2. 设计目标与边界

### 2.1 In Scope

- 闭合 1.3 节 P0/P1 全部缺口
- 让"列表 → 详情 → 实时日志流"这条主路径 **100% e2e 覆盖**
- RunHistoryPage / RunDetailPage / 3 个 run 组件加 Vitest 单测
- 手工触发模态框的 error 改 toast 反馈
- 补充 e2e 烟雾测试

### 2.2 Out of Scope(推迟或独立)

- **运行历史导出 / 报表** — v0.3+ 议题
- **节点级别重试**(目前只能整体重试) — v0.3
- **运行对比 / diff** — v1+ 议题
- **运行模板 / 定时模板** — 后续运营工作流
- **AI 自动解析运行错误** — §11 内容

### 2.3 设计原则

- **测试先行于打磨**:补测试 > 补 UI 动效
- **不重写已能用的代码**:RunStatusBadge / RunTimeline 已合格,零重构
- **M4 基础件复用**:所有 error / confirm 走 notify + useConfirm
- **SSE 是补集,不是替代**:5s 轮询仍保留(防 EventSource 漏掉终端事件)

---

## 3. 里程碑(Milestones)

### M1 — 单元测试基础设施 + RunStatusBadge / RunTimeline 单测(P0,~半天 CC)

**交付物**:
- `__tests__/runs/RunStatusBadge.test.tsx` — 6 个 status × 文案/颜色映射
- `__tests__/runs/RunTimeline.test.tsx` — 节点渲染 / 状态颜色 / 排序 / 空态

**验收**:
- `npx vitest run` 含新测试且全绿

### M2 — LogStreamViewer 单测 + RunHistoryPage/RunDetailPage 流程测试(P0,~1 天 CC)

**交付物**:
- `__tests__/runs/LogStreamViewer.test.tsx`
  - EventSource 模拟(mock EventSource)
  - 2000 事件上限测试
  - 暂停/继续切换
  - 自动滚到底 vs 用户滚走后不自动滚
  - 重连按钮触发新 EventSource
  - 错误状态显示
- `__tests__/pages/RunHistoryPage.test.tsx`
  - 列表渲染 / 筛选 / 分页
  - 手工触发模态框开关 + 触发成功跳转
  - 触发失败显示错误
- `__tests__/pages/RunDetailPage.test.tsx`
  - 加载 → 显示元数据 + 时间线 + 日志
  - 重试用 useConfirm
  - 终端状态停止轮询

**验收**:
- 全部新测试通过
- 覆盖率 report 显示 runs/ + pages/ 提升

### M3 — e2e 烟雾测试 + 手工触发模态框 toast 化(P1,~1 天 CC)

**交付物**:
- `e2e/runs-list.spec.ts` — 加载列表 / 按状态筛选 / 点行进入详情
- `e2e/run-detail.spec.ts` — 详情页加载 / 重试 confirm 流程
- `e2e/run-manual-trigger.spec.ts` — 手工触发模态框 → 触发成功跳转
- `RunHistoryPage` TriggerModal 改 toast.error(原 inline error 改 toast)

**验收**:
- `npx playwright test` 包含新 e2e 且全绿
- TriggerModal 不再有 inline error div,改 toast 反馈

### M4 — 质量打磨 + 文档(P2-P3,~半天 CC)

**交付物**:
- 详情页加 Esc 回列表 / Ctrl+R 重试键盘快捷键
- 详情页"刷新页面"刷新当前 Run 行
- LogStreamViewer 暂停时 header 加视觉提示(背景色变化)
- 后端 SSE 集成测试(`handler_test.go` 加 SSE frame 解析测试)
- FE2_PLAN.md 更新 + TODOS.md 加 fe2 章节
- 提交

**验收**:
- 全部测试通过
- FE2_PLAN.md 含 GSTACK REVIEW REPORT
- TODOS.md 标记 FE2 完成

---

## 4. 测试计划

### 4.1 单元测试(Vitest)

**目标覆盖率**:runs/ + pages/ ≥ 80%

| 文件 | 关键覆盖 |
|------|---------|
| `RunStatusBadge.test.tsx` | 6 个 status 文案 + 颜色 |
| `RunTimeline.test.tsx` | 节点渲染 / 排序 / 空态 |
| `LogStreamViewer.test.tsx` | EventSource mock / 2000 上限 / 暂停 / 自动滚 / 重连 / 错误 |
| `RunHistoryPage.test.tsx` | 列表 / 筛选 / 分页 / 触发模态框 |
| `RunDetailPage.test.tsx` | 加载 / 重试 confirm / 轮询停止 |

### 4.2 E2E 测试(Playwright)

```
e2e/
  runs-list.spec.ts          # 列表加载 + 筛选 + 跳转
  run-detail.spec.ts         # 详情加载 + 重试 confirm
  run-manual-trigger.spec.ts # 手工触发模态框 + 跳转
```

### 4.3 后端集成测试(Go test)

```
TestSSE_RunEvents          # 启动 run 后订阅 SSE,断言收到 node.* / run.completed 事件
TestSSE_Reconnect          # 客户端断开再订阅,从 lastEventId 续传
TestSSE_Backpressure       # 高频事件不丢
```

---

## 5. 风险与对策

| 风险 | 概率 | 影响 | 对策 |
|------|------|------|------|
| EventSource 在 jsdom 不支持 | 高 | LogStreamViewer 单测写不动 | polyfill / mock EventSource |
| 触发模态框改 toast 后漏掉表单错误显示 | 中 | 用户看不到选错方案 | TriggerModal 内联 validation 仍保留 |
| 2000 事件上限改 5000 影响性能 | 中 | 日志流卡顿 | 默认 2000 不动,可后续加配置 |
| e2e 依赖真实 SSE 流 | 中 | 不可重复 | mock EventSource on page |

---

## 6. 验收清单(Definition of Done)

- [ ] M1-M4 全部交付
- [ ] `npx vitest run` 通过,runs/ + pages/ 覆盖率 ≥ 60%
- [ ] `npx playwright test` 全绿(含新 3 个 e2e)
- [ ] 后端 `go test ./internal/executor/...` 全绿
- [ ] 浏览器手测:列表 → 详情 → 日志流 → 重试 confirm
- [ ] TriggerModal 错误改 toast 反馈
- [ ] TODOS.md 加 fe2 完成记录
- [ ] FE2_PLAN.md 末尾加 GSTACK REVIEW REPORT

---

## 7. 时间表

| 阶段 | 工作 | 累计 |
|------|------|------|
| W1 D1-2 | M1 + M2 单测 | ~2 人天 |
| W1 D3-4 | M3 e2e + TriggerModal 改造 | ~2 人天 |
| W1 D5 | M4 打磨 + 文档 + 提交 | ~1 人天 |
| **总计** | | **~5 人天** / **~2-3 天 CC 投入** |

---

## 8. 与 INT / FE3 的依赖

### INT 启动前置
- ✅ FE1 完成
- ✅ FE2 单测 + e2e 覆盖(本计划交付)
- ⏳ 跨服务 fixture(启动 backend + frontend 联调,后端 SSE 真实数据流)

### FE3 入口(后续)
- 运行历史导出为报表(Excel/CSV)
- 节点级别重试
- 运行对比

---

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | FE2 范围 + 策略 | 1 | CLEAR | 全栈已就位,本计划补测试 + M4 风格打磨 |
| Codex Review | — | — | 0 | — | 用户跳过 |
| Eng Review | `/plan-eng-review` | M4 已 ship,Eng 顺延至 FE2 | 0 | — | M4 eng review 推动的 P1/P2 修复全完成 |
| Design Review | — | — | 0 | — | 待运行 |
| DX Review | — | — | 0 | — | 待运行 |

**VERDICT:** CEO REVIEW CLEARED — FE2 测试 + TriggerModal toast 化 + 3 e2e 全部交付。建议 FE3 启动前跑 `/plan-eng-review` 验证完整架构 + 测试覆盖。

**模式:** SELECTIVE_EXPANSION | **严重缺口:** 0 | **未解决决策:** 0

**M1-M3 实际交付:**
- 40 个新单测(RunStatusBadge 9 + RunTimeline 6 + LogStreamViewer 11 + RunHistoryPage 8 + RunDetailPage 6)
- 3 个新 e2e(runs-list + run-detail + run-manual-trigger)
- TriggerModal 错误反馈改 toast
- 总测试数:159 unit + 13 e2e 全绿

---

**End of FE2 Plan**
