# FE1 Plan — ReactFlow 策略编排画布

> Generated by Claude Code, 2026-06-15
> Branch: `main` | Mode: SELECTIVE EXPANSION
> Source spec: `/Users/skyzhao/code/sky-project/af/openspec/changes/astock-selector-system/` (§6 ReactFlow 编排画布 + DAG 执行器)

## TL;DR

FE1 的前端脚手架 **基本完成**。这一阶段的工作不是从零搭建画布，而是：
1. **闭合缺口**：补齐前端测试、e2e 验证、DAG 往返测试、生产级打磨
2. **证明走得通**：save → reload → 一致；trial-run 能跑通全部 8 个节点类型
3. **铺好地基**：让后续 FE2（运行列表 + 实时 SSE 日志）能在干净的画布 API 之上扩展

预计 **3-5 天 CC 投入** / **1-2 周人投入**，分 4 个里程碑交付。

---

## 1. 现状审计（Audit Summary）

### 1.1 已存在的 FE1 文件

经过对 `frontend/src/` 的全面审计，FE1 脚手架已具备完整骨架：

| 组件 | 文件 | 行数 | 状态 |
|------|------|------|------|
| Canvas 容器 | `components/canvas/Canvas.tsx` | ~200 | ✅ 完整 |
| 节点渲染器 | `components/canvas/NodeView.tsx` | 82 | ✅ 完整 |
| 边渲染器 | `components/canvas/EdgeView.tsx` | 41 | ✅ 完整 |
| 节点面板 | `components/canvas/NodePalette.tsx` | 70 | ✅ 完整 |
| 配置面板 | `components/canvas/NodeConfigPanel.tsx` | 376 | ✅ 完整 |
| 节点表单 schema | `components/canvas/nodeForms.ts` | 42 | ✅ 完整 |
| 工具栏 | `components/canvas/Toolbar.tsx` | 125 | ✅ 完整 |
| DAG 序列化 | `components/canvas/snapshot.ts` | 13 | ✅ 完整 |
| 状态管理 | `stores/canvasStore.ts` | 232 | ✅ 完整 |
| 类型定义 | `types/orchestrator.ts` | 380 | ✅ 完整 |
| API 客户端 | `api/strategies.ts` | 173 | ✅ 完整 |
| 策略列表页 | `pages/StrategiesPage.tsx` | 365 | ✅ 完整 |
| 新建策略页 | `pages/StrategyNewPage.tsx` | 231 | ✅ 完整 |
| 策略编辑器页 | `pages/StrategyEditorPage.tsx` | 215 | ✅ 完整 |
| 模板库页 | `pages/TemplateGalleryPage.tsx` | 155 | ✅ 完整 |

**结论**：FE1 的 13 个核心文件已完整实现。无需重建。

### 1.2 已实现的能力

- ✅ ReactFlow 画布（Background / Controls / MiniMap / 拖拽 / 选中 / 删除）
- ✅ 8 种节点类型的拖入与配置（每个类型都有专属表单）
- ✅ 节点 + 边的可视化（颜色按类型区分）
- ✅ Zustand 单向数据流（节点 / 边 / 选中 / 保存状态 / 校验状态）
- ✅ DAG 序列化（`dag_json` ↔ ReactFlow 状态）
- ✅ 保存 / 校验 / 试运行三个工具栏动作
- ✅ 5 个内置模板（`createFromTemplate` 一键创建 DAG）
- ✅ 策略 CRUD（列表 / 新建 / 编辑 / 复制 / 删除 / 导入 / 导出）
- ✅ 试运行 banner（`trialRun` 结果展示）
- ✅ 类型完备的 API 客户端 + TS strict

### 1.3 已有的空白（Gaps）

经过差距分析，发现以下必须闭合的空白：

| 缺口 | 严重度 | 影响 |
|------|--------|------|
| **没有前端测试框架** | P0 | 任何画布改动都是裸奔，没有回归保护 |
| **DAG 往返未验证** | P0 | save → reload 后的图是否一致是契约核心 |
| **8 节点 trial-run 端到端验证未跑过** | P0 | 用户拖个图出来按"试运行"不知道会不会炸 |
| **没有 e2e 测试** | P1 | UI 流程（建模板 → 编辑 → 试运行）没有自动化 |
| **错误边界不完整** | P1 | 节点配置表单 JSON.parse 失败被静默吞掉（`FilterForm.tsx:251`） |
| **加载/空态文案不一致** | P2 | 部分页面用"暂无模板"，部分用"加载失败" |
| **a11y 基础** | P2 | 缺少键盘焦点环、ARIA 标签一致性 |
| **删除节点 confirm()** | P2 | `NodeConfigPanel.tsx:47` 用浏览器原生 confirm，需要替换为内嵌确认 UI |
| **没有视觉回归** | P3 | 节点颜色 / 边样式改动不会被发现 |
| **没有 CI 跑前端 build** | P1 | typecheck/lint/build 全靠本地 |

---

## 2. 设计目标与边界

### 2.1 In Scope

- 闭合 1.3 节列出的所有 P0/P1 缺口
- 让"模板 → 编辑 → 保存 → 重载 → 一致 → 试运行"这条主路径 **100% 走通**
- 提供前端单元测试基础设施（Vitest）
- 提供 Playwright e2e 烟雾测试
- 添加 CI 流水线跑前端 typecheck + lint + build

### 2.2 Out of Scope（推迟到 FE2 或后续）

- **运行历史 / 实时日志（SSE 流式）** — FE2 的核心
- **方案发布 / 灰度 / 回滚** — 后续运营工作流
- **节点类型热插拔 / 自定义节点** — v2 才有意义
- **协同编辑（多人同时编辑一个方案）** — Y2+ 才考虑
- **画布版本对比 / 时间线回放** — 历史 diff 是 v2+ 议题
- **AI 自动生成 DAG** — §11 内容，独立阶段

### 2.3 设计原则

- **测试先行于打磨**：补测试 > 补 UI 动效
- **不重写已能用的代码**：审计显示脚手架质量合格，零重构
- **可观测性是契约的一部分**：所有用户操作有 toast / 错误提示
- **错误永远浮到 UI**：禁止静默吞错（必须修 `FilterForm.tsx:251`）

---

## 3. API 契约（FE1 ↔ Backend）

FE1 调用后端 `/api/v1/strategies/*` 全套接口。已存在的接口如下：

### 3.1 策略 CRUD

| Method | Path | 用途 | 前端调用 |
|--------|------|------|---------|
| `GET` | `/strategies` | 列表 + 分页 + 过滤 | `listStrategies({ status, code_like, page, page_size })` |
| `GET` | `/strategies/:id` | 详情 + 当前 `dag_json` | `getStrategy(id)` |
| `POST` | `/strategies` | 创建（空白或模板） | `createStrategy({ name, code?, description?, dag_json })` |
| `PUT` | `/strategies/:id` | 更新 `dag_json` / 名称 / 描述 | `updateStrategy(id, patch)` |
| `DELETE` | `/strategies/:id` | 删除 | `deleteStrategy(id)` |
| `POST` | `/strategies/:id/clone` | 复制 | `cloneStrategy(id)` |
| `GET` | `/strategies/:id/export` | 导出 JSON | `exportStrategy(id)` |
| `POST` | `/strategies/import` | 导入 JSON | `importStrategy(json)` |

### 3.2 模板

| Method | Path | 用途 |
|--------|------|------|
| `GET` | `/strategy-templates` | 列出 5 个内置模板 |
| `GET` | `/strategy-templates/:code` | 获取单个模板 |
| `POST` | `/strategy-templates/:code/create` | 从模板创建策略 |

### 3.3 试运行

| Method | Path | 用途 | 备注 |
|--------|------|------|------|
| `POST` | `/strategies/:id/trial-run` | 跑整个 DAG | `RunContext{ DB: nil, Notify: nil, DryRun: true }` |
| `POST` | `/strategies/:id/trial-run/node/:nodeId` | 单节点试运行 | 用于逐步调试 |

### 3.4 数据契约要点

**DAG JSON Schema**（`internal/orchestrator/types.go` ↔ `frontend/src/types/orchestrator.ts`）：
```typescript
type DAG = {
  nodes: OrchNode[];   // 来自后端 current_version_dag.nodes
  edges: RFEdge[];     // 来自后端 current_version_dag.edges
};

type OrchNode = {
  id: string;          // UUID
  type: NodeType;      // 8 种之一
  subtype?: string;    // 例如 ma, ema, boll
  params?: Record<string, unknown>;
  position?: { x: number; y: number };  // ReactFlow 用
};

type RFEdge = {
  id: string;
  source: string;      // 节点 id
  target: string;      // 节点 id
};
```

**往返保证**：FE1 必须保证 `parseDag(stringifyDag(dag))` 之后节点 / 边 / 位置都一致。

---

## 4. 8 节点类型的 FE1 表单契约

| 节点类型 | subtype 选项 | 表单字段 | 已有后端实现 |
|---------|------------|---------|------------|
| `data_source` | kline / quote / fundamental / news | subtype, stock_codes, period, days | ✅ `executor/nodes/data_source.go` |
| `indicator` | ma / ema / macd / kdj / boll / volume_ratio / turnover_rate | subtype, period | ✅ 7 个 indicator 文件 |
| `filter` | — | field, op, value | ✅ `filter.go` |
| `rank` | — | field, order, top | ✅ `rank.go` |
| `dedupe` | — | key | ✅ `dedupe.go` |
| `session_tag` | — | 无 | ✅ `session_tag.go` |
| `persist` | — | extra_tags | ✅ `persist.go` |
| `notify` | — | channel_type (morning/afternoon/review/alert) | ✅ `notify.go` |

**契约约束**：
- 所有 `params` 必须能在前端 JSON.stringify 后被后端 `json.Unmarshal` 还原
- 未知字段后端忽略（兼容前向兼容）
- 必填字段缺失 → 后端 `Validate()` 报错 → FE1 显示在试运行 banner

---

## 5. 持久化故事（Draft → Active）

FE1 不直接做"发布"动作（这是后续运营工作流）。当前契约：

```
创建（POST /strategies）   ──→  status: "draft"
更新（PUT /strategies/:id） ──→  status: "draft"（覆盖 current_version_dag）
试运行（POST trial-run）    ──→  不改变 status，只跑一遍
[未来发布动作]              ──→  status: "active"
```

**FE1 现状**：保存只覆盖 `current_version_dag`，不切换 status。✅ 正确。

**契约**：
- 保存按钮总是允许（不论 trial-run 是否成功）
- trial-run 失败不阻塞保存
- 试运行 banner 展示结构化错误（节点 ID + 错误原因）

---

## 6. 里程碑（Milestones）

### M1 — 测试基础设施（P0，~1 天 CC）

**交付物**：
- `frontend/vitest.config.ts` 配置
- `frontend/src/setupTests.ts` 全局 mock（axios, react-router, canvasStore）
- 第一个示范单元测试（`canvasStore.test.ts` — 验证 `addNode` 默认参数、`updateNodeData` 不变性、`loadFromDag` 替换行为）
- `frontend/package.json` 加入 `test` / `test:watch` / `coverage` 脚本
- 添加 Vitest 到 devDependencies

**验收**：
- `pnpm test` 在 CI 中通过
- 覆盖率报告可生成（不必立即达标）

### M2 — DAG 往返 + 8 节点 trial-run 验证（P0，~1 天 CC） ✅ 已完成 (2026-06-16)

**实际交付**（与原计划的差异：文件位置和注册方式按代码现实调整）：
- `frontend/src/__tests__/dag-roundtrip.test.ts` — 序列化往返测试（**33 tests**）
  - parseDag/stringifyDag 输入处理 + 往返身份验证
  - 全部 8 个节点类型 × canvas 默认参数
  - 全部 7 个 indicator 子类型
- `backend/internal/executor/nodes/trial_run_integration_test.go` — Trial-run 契约测试（**13 tests**）
  - 全部 8 个节点类型在 `rc.DB=nil / Notify=nil / DataSource=nil` 下的行为
  - data_source / persist / notify 返回 `dry_run=true`（不是 panic）
  - filter / rank / dedupe 在无上游时返回空结果（success）
  - session_tag 发布到 RunContext.Vars
  - indicator 在缺 klines 时返回 ParamError（不是 panic）—— 这是预期行为
- `backend/internal/orchestrator/dag_structural_test.go` — DAG 结构验证补充（**18 tests**）
  - 之前 `types_test.go` 已覆盖：Validate/TopoSort 主路径
  - 新增：disconnected multi-component DAG、self-loop via TopoSort、8 节点类型文档的 trial-run shape acceptance、用户画出的环 → ErrCycle

**与原计划的差异及原因**：
1. **路径变更** `internal/orchestrator/...` → `internal/executor/nodes/...`
   - `nodes` 包 `import orchestrator`，反过来会形成循环依赖
   - external test package (`orchestrator_test`) 可以避免循环，但放在 nodes 包内更贴近被测代码
2. **未注册到 `OrchestratorTestSuite`**：该 Suite 不存在；现有测试是 plain `TestXxx` 函数，跟随既有约定
3. **未走 `Executor.Execute`**：`executor_test.go` 文件头记录的 coordinator race 会让多节点 DAG flaky；通过直接调用 `node.Run()` 绕开，同时精确覆盖 trial-run 真正关心的契约（nil-safety + dry-run summary）

**验收**（全绿）：
- ✅ frontend: `npx vitest run` → 74 tests pass（M1 的 41 + M2 的 33）
- ✅ backend: `go test ./internal/executor/nodes/... ./internal/orchestrator/...` → 279 tests pass
- ✅ `npx tsc --noEmit` clean / `npx eslint` clean / `go vet` clean
- ✅ trial-run 全部 8 个节点类型行为可预测（dry-run 或合法空结果或 ParamError，均无 panic）
- ✅ persist / notify / data_source 节点的 nil-safety 有显式 `require.NotPanics` 守护

### M3 — Playwright e2e + CI（P1，~1-2 天 CC）✅

**交付物**：
- `frontend/playwright.config.ts`
- `frontend/e2e/` 三个烟雾测试（4 个 case）：
  - `create-from-template.spec.ts`：模板库 → 使用 → 编辑器加载
  - `edit-save-reload.spec.ts`：编辑 → 保存 → 重载 → 一致
  - `trial-run.spec.ts`：试运行 → 看到 banner（success + failed）
- `.github/workflows/ci.yml` frontend job：typecheck + lint + build + vitest + playwright
- README 添加本地运行命令

**验收**：
- ✅ e2e 在 headless 模式下全绿（4 passed, ~3.5s）
- ✅ CI 在 PR 时强制跑（ci.yml 已集成）

### M4 — 错误边界 + UI 一致性（P1-P2，CC ~1 天）

> **CEO Review 更新 (2026-06-17):** 审计发现 M4 所有基础件(ConfirmDialog / Toast / ErrorBoundary)均不存在,需新建而非复用。原生 confirm/alert 的散落面是 4 文件 5 处而非 2 处。以下交付物已按实际影响面补全。

**基础件(需从零建,均不存在)**:

| 基础件 | 技术选型 | 挂载点 |
|--------|---------|--------|
| Toast 系统 | react-hot-toast(~5KB,自带 a11y,队列/去重/堆叠) | `<App>` 根,`<RouterProvider>` 外 |
| ErrorBoundary | 自建 class 组件,`componentDidCatch` 渲染 fallback UI | 包裹 `<RouterProvider>`,根级 |
| ConfirmDialog | 自建,`role="dialog"` + Esc 关闭 + 焦点陷阱 + 默认焦点在取消 | 全局,通过 `useConfirm()` hook 调用 |

**修复站点(5 处 alert/confirm + 2 处静默吞错)**:

| 文件:行 | 当前 | 改为 |
|---------|------|------|
| `NodeConfigPanel.tsx:47` | `confirm(\`删除节点 ${node.id}?\`)` | `confirm({ title: "删除节点", message: node.id, danger: true })` |
| `StrategiesPage.tsx:95` | `confirm(\`确认删除方案 "${s.name}"?\`)` | `confirm({ title: "删除方案", message: s.name, danger: true })` |
| `StrategiesPage.tsx:105` | `alert(\`删除失败: ${err...}\`)` | `toast.error(getErrorMessage(err, "删除失败"))` |
| `StrategiesPage.tsx:123` | `alert(\`导出失败: ${err...}\`)` | `toast.error(getErrorMessage(err, "导出失败"))` |
| `RunDetailPage.tsx:78` | `confirm(\`确认重试 Run #${run.id}?\`)` | `confirm({ title: "重试运行", message: \`Run #${run.id}\` })` |
| `RecommendationsPage.tsx:83` | `alert("当前页没有数据可导出")` | `toast.warning("当前页没有数据可导出")` |
| `FilterForm.tsx:250` | `catch {} // ignore parse error` | 显示 inline 红色提示 "JSON 格式错误",不更新 params |
| `StrategyEditorPage.tsx` save catch | `console.error` 吞错 | `toast.error(getErrorMessage(err, "保存失败"))` |

**axios 拦截器统一切入 (client.ts:31)**:
- 保留 `console.error('[api] error:', status, message, url)` 日志
- 追加 `notifyError(err)` —— 所有未在各页 catch 的网络错自动弹 toast
- toast 只显示 `err.response?.data?.message ?? "网络请求失败"`,绝不暴露原始错误栈

**统一三态组件**(抽取 `StrategiesPage` 已有模式):

```tsx
// components/shared/LoadingState.tsx — "加载中…" spinner
// components/shared/EmptyState.tsx  — "暂无数据" + 可选 action
// components/shared/ErrorState.tsx  — "加载失败：[原因]" + 重试按钮
```

**已就绪(wip in index.css)**:
- `:focus-visible` 全局焦点环(2px brand.500, offset 2px)
- `.animate-toast-in` 入场动画(0.18s ease-out)——用于 react-hot-toast 容器样式

**新增单元测试(M4 回归保护)**:

| 测试文件 | 关键覆盖 |
|---------|---------|
| `ConfirmDialog.test.tsx` | 渲染 title/message,确认回调,取消回调,Esc 关闭,焦点在取消 |
| `ErrorBoundary.test.tsx` | 子组件抛错被捕获,fallback UI 渲染,`componentDidCatch` 日志 |
| `FilterForm.test.tsx` | 非法 JSON 输入显示 inline 错误,合法输入正常更新 |
| `toast.test.ts` / `notifyError.test.ts` | `getErrorMessage` 各种 err 类型 → 正确 fallback |

**验收**:
- 静默吞错 = 全部 7 个站点修复
- 所有用户操作都有可见反馈(toast / inline error / dialog)
- `notifyError` + 三态组件覆盖全部 13 个核心组件
- `vitest run` 包含上述 4 个新测试且全绿

### M4-EXT — CEO Review 选择性扩展（全部已采纳,CC ~1 天）

以下 5 项由 `/plan-ceo-review` 提出,已全部纳入范围:

| # | 扩展 | 价值 | 工作量 |
|---|------|------|--------|
| E1 | **Undo toast** — 删除/归档后弹"已删除 [撤销]",5 秒内可撤回,需前端缓存待恢复数据 | 防误删焦虑,核心体验 | CC ~15min |
| E2 | **Toast 接 axios 拦截器** — 已纳入 M4 基线,非额外项 | 全局兜底 | 见 M4 |
| E3 | **`useConfirm()` Promise hook** — `await confirm({...})` 替代回调,调用点更干净 | 代码质量,减少嵌套 | CC ~8min |
| E4 | **键盘快捷键** — Esc 关 dialog、Enter 确认(danger 不 Enter)、画布 Del 删选中节点、Ctrl+S 保存 | Power user 手感 | CC ~10min |
| E5 | **空态引导** — 空方案列表显示"创建第一个方案 →"引导卡 + 建议跳转模板库 | 新用户首次体验 | CC ~15min |

**E1 技术要点**:前端缓存已删对象的浅拷贝 + 5 秒 `setTimeout` 撤销(调 create/clone 恢复);被撤销内容不持久化到服务端缓存,页面刷新后不可撤。

**E4 实现位置**:
- `ConfirmDialog.tsx`: `useEffect` 监听 `keydown` (Escape → onCancel, Enter → danger 时不触发)
- `Canvas.tsx`: 已有 delete 键处理,补充 `useEffect` + `event.key === 'Delete'`
- `useCanvasHotkeys.ts`(新建): Ctrl/Cmd+S → 触发 `canvasStore` 保存

### M5 — 视觉回归（P3，可选，~半天 CC）

**交付物**：
- Playwright 截图测试覆盖 3 个核心页面（列表 / 编辑器 / 模板库）
- 节点 + 边的 baseline 截图（覆盖全部 8 种类型）

**验收**：
- PR 修改节点颜色 → CI 截图 diff 报警（阈值设宽,diff pixel < 1% 通过）

---

## 7. 测试计划

### 7.1 单元测试（Vitest）

**目标覆盖率**：核心 store / 工具函数 ≥ 80%

| 文件 | 关键覆盖 |
|------|---------|
| `canvasStore.test.ts` | `addNode`, `removeNode`, `updateNodeData`, `loadFromDag`, `markSaved`, `markDirty` |
| `dag-roundtrip.test.ts` | `parseDag ∘ stringifyDag = identity`（空 / 单节点 / 多节点 / 边） |
| `orchestrator-types.test.ts` | 8 种 NodeType 的 `defaultParamsFor` 都返回合法对象 |
| `strategyValidation.test.ts` | 客户端校验逻辑（至少一个 data_source + 一个 persist） |
| `ConfirmDialog.test.tsx` | 渲染 title/message,确认回调,取消回调,Esc 关闭,Enter 焦点行为,danger 态 Enter 不触发 |
| `ErrorBoundary.test.tsx` | 子组件抛错被捕获,fallback UI 渲染,`componentDidCatch` 日志 |
| `FilterForm.test.tsx` | 非法 JSON 输入显示 inline 错误,合法输入正常更新 |
| `notifyError.test.ts` | `getErrorMessage` 各 err 类型映射到正确中文 fallback |

### 7.2 后端集成测试（Go test）

`backend/internal/orchestrator/integration_trial_run_test.go`：

```
TestTrialRun_AllNodeTypes  // 8 个节点 × 1 个 case
TestTrialRun_EmptyDAG      // 边界：空图返回什么
TestTrialRun_Disconnected  // 边界：孤立节点
TestTrialRun_Cycle         // 边界：环（DAG 不应该有，但试运行应该优雅报错）
TestTrialRun_PersistShortCircuits  // 短路：DB=nil
TestTrialRun_NotifyShortCircuits   // 短路：Notify=nil
```

### 7.3 E2E 测试（Playwright）

```
e2e/
  create-from-template.spec.ts   // 端到端模板创建
  edit-save-reload.spec.ts        // 保存往返
  trial-run-banner.spec.ts        // 试运行 UI
  keyboard-navigation.spec.ts     // Tab 焦点顺序
  empty-states.spec.ts            // 加载/空/错误三态
```

### 7.4 CI 流水线

```yaml
# .github/workflows/frontend.yml
name: frontend
on: [pull_request]
jobs:
  test:
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20 }
      - run: cd frontend && npm ci
      - run: cd frontend && npm run typecheck
      - run: cd frontend && npm run lint
      - run: cd frontend && npm run test -- --coverage
      - run: cd frontend && npm run build
      - run: cd frontend && npx playwright install --with-deps
      - run: cd frontend && npx playwright test
```

---

## 8. 风险与对策

| 风险 | 概率 | 影响 | 对策 |
|------|------|------|------|
| DAG 往返丢字段（例如 ReactFlow position） | 中 | 用户编辑位置不保留 | ✅ M2 已测：`dag-roundtrip.test.ts` 33 tests |
| trial-run 8 节点有 panic 路径 | 中 | 试运行 banner 不可用 | ✅ M2 已测：`trial_run_integration_test.go` 13 tests，含 `require.NotPanics` 守护 |
| 测试基础设施拖慢 CI | 低 | 速度变慢 | 用 Vitest 而非 Jest（更快） |
| 视觉回归导致 PR 噪音 | 中 | M5 可选推迟 | 截图阈值设宽（diff pixel < 1% 通过） |
| e2e 依赖外部数据源（A股实时行情） | 高 | 测试 flaky | 试运行用 mock，模板用 fixture |

---

## 9. 与 FE2 / INT 的依赖

### FE2 启动前置（本计划完成时必须就绪）
- ✅ 策略 CRUD 完整（FE2 在此之上做"运行历史"）
- ✅ DAG 序列化稳定（FE2 的 SSE 事件需要节点 ID 关联）
- ✅ 类型完备（FE2 会引入 `RunLog`, `RunStatus`, `SSE EventType`，都在 `orchestrator.ts` 已经定义）
- ⏳ 后端 SSE endpoint（FE2 启动时 FE1 即可测试 mock）

### INT 启动前置
- ✅ M3 e2e 烟雾测试通过
- ✅ CI 跑前端 + 后端
- ⏳ Playwright 跨服务 fixture（启动 backend on :8080 + frontend on :5173）

---

## 10. 验收清单（Definition of Done）

FE1 在以下全部为真时视为完成：

- [ ] M1-M4 + M4-EXT 全部交付
- [ ] 附录 A 列出的全部 7 个 bug 已修复
- [ ] `pnpm test` 在 CI 中通过,覆盖率 ≥ 60%（store + utils + M4 基础件）
- [ ] M4 新增 4 个单元测试(ConfirmDialog / ErrorBoundary / FilterForm / notifyError)全绿
- [ ] `pnpm build` 无 TS 错误
- [ ] `npx playwright test` 全绿
- [ ] 8 节点 trial-run 后端集成测试全绿
- [ ] DAG 往返测试全绿
- [ ] 浏览器手测一遍：模板 → 编辑 → 保存 → 重载 → 试运行 → 看 banner
- [ ] 手测附录 A 全部 7 个站点：弹 toast/dialog 而非浏览器原生弹出,FilterForm 输错有红色提示
- [ ] Undo toast 可撤回删除(E1)
- [ ] Esc/Enter/Delete 键盘快捷键可用(E4)
- [ ] 空方案列表显示引导卡(E5)
- [ ] TODOS.md 添加 "FE1 v0.1 shipped" 完成记录
- [ ] PR 合入 main 后 frontend deploy job 跑通

---

## 11. 时间表

| 周次 | 工作 | 累计 |
|------|------|------|
| W1 D1-2 | M1 测试基础设施 + M2 集成测试 | ~2 人天 |
| W1 D3-5 | M3 e2e + CI 流水线 | ~3 人天 |
| W2 D1-3 | M4 错误边界 + UI 一致性 + M4-EXT 扩展 | ~5 人天 |
| W2 D4 | M5 视觉回归（可选） | +0.5 人天 |
| **总计** | | **~10.5 人天** / **~4-5 天 CC 投入** |

---

## 12. 后续（FE2 入口）

FE1 完工后，下一阶段自然衔接：
1. `pages/RunsPage.tsx` — 方案运行历史列表
2. `pages/RunDetailPage.tsx` — 单次运行详情 + SSE 日志
3. `components/runs/RunStatusBadge.tsx` — 状态指示器
4. `api/runs.ts` — 运行历史 API 客户端
5. `hooks/useRunStream.ts` — EventSource 封装

但这些属于 FE2 范围，本计划不展开。

---

## 附录 A：审计发现的具体 bug（需修复 — CEO Review 更新至 7 项）

1. **`NodeConfigPanel.tsx:47`** — 浏览器 `confirm()`,阻塞线程且样式不可控。**修复**:内嵌 ConfirmDialog + `useConfirm` hook。
2. **`FilterForm.tsx:250`** — `JSON.parse` 失败被空 catch,用户零反馈。**修复**:inline 红色 "JSON 格式错误"。
3. **`StrategyEditorPage.tsx`** — Save 错误仅 `console.error`,用户看不到。**修复**:`toast.error(getErrorMessage(...))`。
4. **`canvasStore.ts`** — 没有 mutation 防护。**修复**:所有 setter 用 immutable update。
5. **`StrategiesPage.tsx:105,123`** — 删除/导出失败用原生 `alert()`,无法样式化。**修复**:toast.error。
6. **`RunDetailPage.tsx:78`** — 重试用原生 `confirm()`。**修复**:`useConfirm()` hook。
7. **`RecommendationsPage.tsx:83`** — 空数据导出的提示用原生 `alert()`。**修复**:toast.warning。

> ~~5. trial-run 后端 nil-safety~~ → ✅ M2 已验证(`trial_run_integration_test.go:13 tests, require.NotPanics`)。

## 附录 B：现有约定（保留）

- API 基础路径：`/api/v1`
- 响应包络：`{ code, data, message? }`（已有 axios 拦截器处理）
- 错误优先抛出 `Error.message`，由 toast 组件渲染
- 命名约定：组件 PascalCase，store camelCase + `Store` 后缀
- TypeScript 严格模式开启
- Tailwind + 自定义 `btn-primary` / `btn-secondary` / `form-input` 类
- 中文文案优先（产品定位中国市场）

## 附录 C：相关文件位置

- 前端：`/Users/skyzhao/code/sky-project/af/frontend/src/`
- 后端 orchestrator：`/Users/skyzhao/code/sky-project/af/backend/internal/orchestrator/`
- 后端 executor nodes：`/Users/skyzhao/code/sky-project/af/backend/internal/executor/nodes/`
- 配置模板：`/Users/skyzhao/code/sky-project/af/backend/configs/config.example.yaml`
- TODOS：`/Users/skyzhao/code/sky-project/af/TODOS.md`
- 内存笔记：`/Users/skyzhao/.claude/projects/-Users-skyzhao/memory/astock-af-project.md`

---

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | M4+M5 剩余范围 + 策略审核 | 1 | CLEAR | M4 基础件需新建(非复用),bug 面 7 处(非 2 处),5 项扩展全部采纳 |
| Codex Review | — | — | 0 | — | 用户跳过 |
| Eng Review | — | — | 0 | — | 待运行 |
| Design Review | — | — | 0 | — | 待运行 |
| DX Review | — | — | 0 | — | 待运行 |

**VERDICT:** CEO REVIEW CLEARED — M4+M4-EXT 基线就绪,7 项 bug 全部覆盖,5 项扩展已纳入范围。建议 `/plan-eng-review` 验证架构 + 测试完整性后再实现。

**模式:** SELECTIVE_EXPANSION | **严重缺口:** 0 | **未解决决策:** 0 | **扩展采纳:** 5/5

**End of FE1 Plan**
